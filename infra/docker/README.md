# infra/docker

Shared/reference Dockerfiles live alongside their services (`apps/api/Dockerfile`,
`apps/ai-worker/Dockerfile`). This folder is reserved for any cross-cutting Docker assets (e.g. a shared
base image) introduced in later phases.
