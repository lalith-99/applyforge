# infra/railway

Railway deployment configuration for `apps/api` and `apps/ai-worker`. Not configured yet — see
[docs/DEPLOYMENT.md](../../docs/DEPLOYMENT.md). Will be added once Phase 1 introduces functionality worth
deploying.
