# packages/contracts

Shared API contract artifacts (OpenAPI schema generated from the Go API, and any generated TypeScript
types consumed by `apps/web`). Empty until Phase 1 introduces the first real endpoints.
